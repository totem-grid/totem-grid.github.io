---
import IndexLayout from '@/layouts/IndexLayout.astro'
import { themeConfig } from '@/config'
---

<IndexLayout
  title="Manifesto - Totem Grid"
  description="We are all connected. Evolving how we organize, communicate, and create value. A manifesto for authentic connection and systemic renewal."
>
  <main class="manifesto-page">
    <div class="manifesto-content">
      <!-- Hero Section -->
      <section class="manifesto-hero">
        <div class="hero-badge">
          <span class="badge-text">📜 Our Vision</span>
        </div>
        <h1 class="hero-title">We are all connected</h1>
        <p class="hero-subtitle">Evolving How We Organize, Communicate, and Create Value</p>
      </section>

      <!-- Questions Section -->
      <section class="questions-section">
        <div class="questions-content">
          <div class="question-card">
            <p class="question-text">
              <strong
                >What if the technology we rely on to connect is fundamentally disconnecting us?</strong
              >
            </p>
          </div>

          <div class="question-card">
            <p class="question-text">
              <strong
                >What if our organizations' hidden norms sabotage our greatest potential?</strong
              >
            </p>
          </div>

          <div class="question-card">
            <p class="question-text">
              <strong
                >What if there's a peaceful way for humanity to evolve beyond our current
                predicament?</strong
              >
            </p>
          </div>
        </div>
      </section>

      <!-- Call to Action Section -->
      <section class="cta-section">
        <div class="cta-content">
          <div class="cta-text">
            <p class="cost-statement">
              <strong
                >The cost of continuing along the current path is not just disconnected
                networks—it's disconnected lives, lost potential, and forgotten humanity</strong
              >
            </p>

            <p class="tipping-point">
              <strong
                >We stand at a tipping point—where our choices today will either deepen isolation or
                foster genuine human connection.</strong
              >
            </p>

            <p class="path-forward">
              <strong
                >This manifesto charts the path toward authentic connection and systemic renewal.</strong
              >
            </p>
          </div>

          <div class="manifesto-cta">
            <a
              href="/we-are-all-connected-totem-grid-manifesto.pdf"
              target="_blank"
              class="read-btn"
            >
              <span class="btn-text">Read Manifesto</span>
              <span class="btn-icon">📄</span>
            </a>
            <p class="cta-note">Opens PDF in new tab</p>
          </div>
        </div>
      </section>

      <!-- Connection Visual -->
      <section class="connection-visual">
        <div class="visual-content">
          <div class="network-visualization">
            <div class="network-node" data-node="1"></div>
            <div class="network-node" data-node="2"></div>
            <div class="network-node" data-node="3"></div>
            <div class="network-node" data-node="4"></div>
            <div class="network-node" data-node="5"></div>
            <div class="network-node" data-node="6"></div>
            <svg class="connection-lines" viewBox="0 0 400 300">
              <line class="connection-line" x1="100" y1="80" x2="200" y2="120"></line>
              <line class="connection-line" x1="200" y1="120" x2="300" y2="80"></line>
              <line class="connection-line" x1="120" y1="200" x2="200" y2="120"></line>
              <line class="connection-line" x1="200" y1="120" x2="280" y2="200"></line>
              <line class="connection-line" x1="100" y1="80" x2="120" y2="200"></line>
              <line class="connection-line" x1="300" y1="80" x2="280" y2="200"></line>
            </svg>
          </div>
          <p class="visual-caption">
            True connection emerges when we embrace our interconnectedness
          </p>
        </div>
      </section>
    </div>
  </main>
</IndexLayout>

<style>
  .manifesto-page {
    max-width: 64rem;
    margin: 0 auto;
    padding: 0 1rem;
  }

  .manifesto-content {
    width: 100%;
  }

  /* Hero Section */
  .manifesto-hero {
    text-align: center;
    padding: 3rem 0 5rem;
    background: linear-gradient(
      135deg,
      rgba(102, 126, 234, 0.05) 0%,
      rgba(118, 75, 162, 0.05) 100%
    );
    border-radius: 2rem;
    margin-bottom: 4rem;
    position: relative;
    overflow: hidden;
  }

  .manifesto-hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 120%;
    height: 100%;
    background: radial-gradient(ellipse at center, rgba(102, 126, 234, 0.08) 0%, transparent 70%);
    pointer-events: none;
    z-index: -1;
  }

  .hero-badge {
    display: inline-block;
    margin-bottom: 1.5rem;
    animation: badgeSlideIn 0.8s ease-out 0.2s both;
  }

  .badge-text {
    display: inline-block;
    padding: 0.5rem 1.25rem;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border-radius: 2rem;
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: 0.025em;
    box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
  }

  .hero-title {
    font-size: 4rem;
    font-weight: 800;
    color: var(--color-text-primary);
    margin-bottom: 1rem;
    line-height: 1.1;
    animation: titleSlideUp 0.8s ease-out 0.4s both;
    background: linear-gradient(135deg, var(--color-text-primary) 0%, #667eea 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: 1.5rem;
    line-height: 1.4;
    color: var(--color-text-secondary);
    margin: 0;
    font-weight: 500;
    animation: subtitleSlideUp 0.8s ease-out 0.6s both;
  }

  /* Questions Section */
  .questions-section {
    padding: 4rem 0;
    margin-bottom: 4rem;
  }

  .questions-content {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
    max-width: 48rem;
    margin: 0 auto;
  }

  .question-card {
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-left: 4px solid #667eea;
    border-radius: 0.75rem;
    padding: 2rem;
    transition: all 0.3s ease;
    animation: cardSlideUp 0.8s ease-out;
    animation-fill-mode: both;
  }

  .question-card:nth-child(1) {
    animation-delay: 0.1s;
  }
  .question-card:nth-child(2) {
    animation-delay: 0.3s;
  }
  .question-card:nth-child(3) {
    animation-delay: 0.5s;
  }

  .question-card:hover {
    transform: translateX(5px);
    box-shadow: 0 10px 25px rgba(102, 126, 234, 0.15);
    border-left-color: #764ba2;
  }

  .question-text {
    font-size: 1.2rem;
    line-height: 1.6;
    color: var(--color-text-primary);
    margin: 0;
  }

  /* CTA Section */
  .cta-section {
    padding: 4rem 0;
    margin-bottom: 4rem;
    background: linear-gradient(
      135deg,
      rgba(102, 126, 234, 0.03) 0%,
      rgba(118, 75, 162, 0.03) 100%
    );
    border-radius: 2rem;
  }

  .cta-content {
    max-width: 48rem;
    margin: 0 auto;
    text-align: center;
  }

  .cta-text {
    margin-bottom: 3rem;
  }

  .cost-statement,
  .tipping-point,
  .path-forward {
    font-size: 1.25rem;
    line-height: 1.7;
    margin-bottom: 2rem;
    color: var(--color-text-primary);
  }

  .cost-statement {
    color: #ef4444;
  }

  .tipping-point {
    color: #f59e0b;
  }

  .path-forward {
    color: #10b981;
    margin-bottom: 0;
  }

  .manifesto-cta {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .read-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1.25rem 2.5rem;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    text-decoration: none;
    border-radius: 0.75rem;
    font-size: 1.2rem;
    font-weight: 600;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
    box-shadow: 0 8px 25px rgba(102, 126, 234, 0.3);
  }

  .read-btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: left 0.5s;
  }

  .read-btn:hover::before {
    left: 100%;
  }

  .read-btn:hover {
    transform: translateY(-3px);
    box-shadow: 0 12px 35px rgba(102, 126, 234, 0.4);
  }

  .btn-icon {
    font-size: 1.5rem;
    transition: transform 0.3s ease;
  }

  .read-btn:hover .btn-icon {
    transform: scale(1.1);
  }

  .cta-note {
    font-size: 0.875rem;
    color: var(--color-text-secondary);
    margin: 0;
    font-style: italic;
  }

  /* Connection Visual */
  .connection-visual {
    padding: 4rem 0;
    text-align: center;
  }

  .visual-content {
    max-width: 32rem;
    margin: 0 auto;
  }

  .network-visualization {
    position: relative;
    width: 400px;
    height: 300px;
    margin: 0 auto 2rem;
    max-width: 100%;
  }

  .network-node {
    position: absolute;
    width: 20px;
    height: 20px;
    background: linear-gradient(135deg, #667eea, #764ba2);
    border-radius: 50%;
    animation: nodePulse 2s ease-in-out infinite;
    box-shadow: 0 0 10px rgba(102, 126, 234, 0.5);
  }

  .network-node[data-node='1'] {
    top: 20%;
    left: 25%;
    animation-delay: 0s;
  }

  .network-node[data-node='2'] {
    top: 30%;
    left: 50%;
    animation-delay: 0.3s;
  }

  .network-node[data-node='3'] {
    top: 20%;
    right: 25%;
    animation-delay: 0.6s;
  }

  .network-node[data-node='4'] {
    bottom: 30%;
    left: 30%;
    animation-delay: 0.9s;
  }

  .network-node[data-node='5'] {
    bottom: 30%;
    right: 30%;
    animation-delay: 1.2s;
  }

  .network-node[data-node='6'] {
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    animation-delay: 1.5s;
    width: 24px;
    height: 24px;
  }

  .connection-lines {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
  }

  .connection-line {
    stroke: rgba(102, 126, 234, 0.6);
    stroke-width: 2;
    stroke-dasharray: 5, 5;
    animation: lineFlow 3s linear infinite;
  }

  .visual-caption {
    font-size: 1.1rem;
    line-height: 1.6;
    color: var(--color-text-secondary);
    font-style: italic;
    margin: 0;
  }

  /* Animations */
  @keyframes badgeSlideIn {
    from {
      opacity: 0;
      transform: translateY(-20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes titleSlideUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes subtitleSlideUp {
    from {
      opacity: 0;
      transform: translateY(25px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes cardSlideUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes nodePulse {
    0%,
    100% {
      opacity: 1;
      transform: scale(1);
    }
    50% {
      opacity: 0.7;
      transform: scale(1.2);
    }
  }

  @keyframes lineFlow {
    0% {
      stroke-dashoffset: 0;
    }
    100% {
      stroke-dashoffset: 20;
    }
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .manifesto-hero {
      padding: 2rem 1rem 3rem;
      margin-bottom: 3rem;
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .hero-subtitle {
      font-size: 1.25rem;
    }

    .questions-content {
      gap: 1.5rem;
    }

    .question-card {
      padding: 1.5rem;
    }

    .question-text {
      font-size: 1.1rem;
    }

    .cost-statement,
    .tipping-point,
    .path-forward {
      font-size: 1.1rem;
    }

    .read-btn {
      width: 100%;
      max-width: 280px;
      justify-content: center;
    }

    .network-visualization {
      width: 300px;
      height: 225px;
    }

    .visual-caption {
      font-size: 1rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-badge,
    .hero-title,
    .hero-subtitle,
    .question-card {
      animation: none;
    }

    .network-node,
    .connection-line {
      animation: none;
    }

    .question-card,
    .read-btn,
    .btn-icon {
      transition: none;
    }

    .read-btn::before {
      transition: none;
    }
  }
</style>
