---
import IndexLayout from '@/layouts/IndexLayout.astro'
import { themeConfig } from '@/config'
---

<IndexLayout title="Contribute - Totem Grid Collective" description="Join our collective revolution. Redefining trust online starts with redefining trust in the organizations behind the protocols.">
  <main class="contribute-page">
    <div class="contribute-content">

      <!-- Hero Section -->
      <section class="contribute-hero">
        <div class="hero-badge">
          <span class="badge-text">🚀 Join the Revolution</span>
        </div>
        <h1 class="hero-title">Totem Grid Collective</h1>
        <p class="hero-subtitle">
          reinventing the way humanity connects, organizes, and creates value
        </p>

        <div class="hero-cta">
          <button class="explore-btn" id="explore-btn">
            <span class="btn-text">Start Exploring</span>
            <span class="btn-arrow">→</span>
          </button>
        </div>
      </section>

      <!-- Join Revolution Section -->
      <section class="revolution-section">
        <h2 class="section-title">Join our Collective Revolution</h2>

        <div class="revolution-content">
          <p class="revolution-text">
            Redefining trust online starts with redefining trust in the organizations behind the protocols.
            Our innovative model embodies the future by fusing the perfect mix of bleeding-edge open-source
            social and internet technologies online today. As an Internet-Native company, our collective
            embodies the original values of the internet as outlined in 'A Declaration of the Independence
            of Cyberspace'—open collaboration, decentralization, personal sovereignty, and community-driven
            innovation, unlocking 'the power of passionate creatives' at scale.
          </p>

          <p class="revolution-text">
            We're solving urgent, global problems, beginning with a safe, fun, and human-centered network
            for the trillion-dollar live events market, and scaling toward transforming how the internet
            and organizations everywhere operate. We're pioneering something bigger than 'jobs'—we're
            creating a decentralized collective where passion, innovation, and equity intersect, enabling
            part-time contributions that align authentically with your life and values.
          </p>

          <div class="revolution-highlight">
            <p class="highlight-text">
              <strong>Totem Grid builds human-centered networks that diffuse sovereignty, enabling humanity to dance more freely.</strong>
            </p>
          </div>
        </div>
      </section>

      <!-- Productize Sovereignty Section -->
      <section class="sovereignty-section">
        <div class="sovereignty-card">
          <h2 class="sovereignty-title">
            <strong>Productize diffuse sovereignty with us</strong>
          </h2>
        </div>
      </section>

      <!-- Three Pillars Section -->
      <section class="pillars-section">
        <div class="pillars-grid">

          <div class="pillar-card">
            <div class="pillar-icon">🏗️</div>
            <h3 class="pillar-title">Building a better internet for humanity begins by building better organizations.</h3>
            <p class="pillar-description">
              At Totem Grid, we combine <strong>Holacracy's structured self-management</strong> with
              <strong>Teal's principles of wholeness, evolutionary purpose, and autonomy</strong>, all
              anchored by transparent <strong>on-chain governance</strong>. This unique fusion empowers
              every contributor to define their roles, make meaningful decisions, and shape their path
              in alignment with our shared mission.
            </p>
            <p class="pillar-description">
              We're entrepreneurial partners who share ownership and opportunity equally, guided by
              clarity and mutual trust. Together, we're embodying the kind of network we envision for
              the world: decentralized, human-centered, and purpose-driven.
            </p>
          </div>

          <div class="pillar-card">
            <div class="pillar-icon">💰</div>
            <h3 class="pillar-title">Real-World Impact, Trillion-Dollar Market Potential</h3>
            <p class="pillar-description">
              Our first step—enabling resilient, peer-to-peer communication at festivals—addresses urgent
              problems in the global live-events industry, a <strong>trillion-dollar market</strong> primed
              for transformation. As Totem Grid scales, founding partners share in the substantial value
              created, benefiting directly from our collective success in redefining how millions of people
              connect worldwide.
            </p>
          </div>

          <div class="pillar-card">
            <div class="pillar-icon">🎯</div>
            <h3 class="pillar-title">Mission Beyond Profits</h3>
            <p class="pillar-description">
              We're driven to solve meaningful human problems—like keeping event-goers safe and connected
              through decentralized coordination technology—while simultaneously providing a practical
              template for communities to build and thrive without surveillance or exploitation.
            </p>
          </div>

        </div>
      </section>

      <!-- What to Expect Section -->
      <section class="expect-section">
        <h2 class="section-title">What to Expect in Explorations</h2>

        <div class="expect-grid">

          <div class="expect-card">
            <div class="expect-icon">💬</div>
            <h3 class="expect-title">Initial Conversations</h3>
            <p class="expect-description">
              This is not a traditional interview. It's a series of candid chats designed to understand
              how your skills and passions align with our mission, clarify any questions you might have
              about our unique model, and explore how a contribution could fit into your life.
            </p>
          </div>

          <div class="expect-card">
            <div class="expect-icon">🔍</div>
            <h3 class="expect-title">Transparent Next Steps</h3>
            <p class="expect-description">
              After our call, you'll receive clear, detailed materials outlining how we equitably share
              value through our collective, what specific contribution paths look like, and what you can
              expect as a founding partner.
            </p>
          </div>

        </div>
      </section>

      <!-- Final CTA Section -->
      <section class="final-cta">
        <div class="cta-card">
          <h2 class="cta-title">Ready to Shape the Future?</h2>
          <p class="cta-subtitle">
            Join a collective that's redefining how humanity connects and organizes
          </p>
          <button class="contact-btn" id="contact-btn">
            <span class="btn-text">Start the Conversation</span>
            <span class="btn-arrow">→</span>
          </button>
        </div>
      </section>

    </div>
  </main>
</IndexLayout>

<style>
  .contribute-page {
    max-width: 64rem;
    margin: 0 auto;
    padding: 0 1rem;
  }

  .contribute-content {
    width: 100%;
  }

  /* Hero Section */
  .contribute-hero {
    text-align: center;
    padding: 3rem 0 5rem;
    background: linear-gradient(
      135deg,
      rgba(102, 126, 234, 0.05) 0%,
      rgba(118, 75, 162, 0.05) 100%
    );
    border-radius: 2rem;
    margin-bottom: 4rem;
    position: relative;
    overflow: hidden;
  }

  .contribute-hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 120%;
    height: 100%;
    background: radial-gradient(ellipse at center, rgba(102, 126, 234, 0.08) 0%, transparent 70%);
    pointer-events: none;
    z-index: -1;
  }

  .hero-badge {
    display: inline-block;
    margin-bottom: 1.5rem;
    animation: badgeSlideIn 0.8s ease-out 0.2s both;
  }

  .badge-text {
    display: inline-block;
    padding: 0.5rem 1.25rem;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border-radius: 2rem;
    font-size: 0.875rem;
    font-weight: 500;
    letter-spacing: 0.025em;
    box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
  }

  .hero-title {
    font-size: 3.5rem;
    font-weight: 800;
    color: var(--color-text-primary);
    margin-bottom: 1rem;
    line-height: 1.1;
    animation: titleSlideUp 0.8s ease-out 0.4s both;
    background: linear-gradient(135deg, var(--color-text-primary) 0%, #667eea 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: 1.5rem;
    line-height: 1.4;
    color: var(--color-text-secondary);
    margin-bottom: 2.5rem;
    font-style: italic;
    animation: subtitleSlideUp 0.8s ease-out 0.6s both;
  }

  .hero-cta {
    animation: ctaSlideUp 0.8s ease-out 0.8s both;
  }

  /* Buttons */
  .explore-btn,
  .contact-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1.25rem 2.5rem;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border: none;
    border-radius: 0.75rem;
    font-size: 1.1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
  }

  .explore-btn::before,
  .contact-btn::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: left 0.5s;
  }

  .explore-btn:hover::before,
  .contact-btn:hover::before {
    left: 100%;
  }

  .explore-btn:hover,
  .contact-btn:hover {
    transform: translateY(-3px);
    box-shadow: 0 10px 30px rgba(102, 126, 234, 0.4);
  }

  .btn-arrow {
    transition: transform 0.3s ease;
  }

  .explore-btn:hover .btn-arrow,
  .contact-btn:hover .btn-arrow {
    transform: translateX(3px);
  }

  /* Revolution Section */
  .revolution-section {
    padding: 4rem 0;
    margin-bottom: 4rem;
  }

  .section-title {
    font-size: 2.5rem;
    font-weight: 700;
    color: var(--color-text-primary);
    text-align: center;
    margin-bottom: 3rem;
    line-height: 1.2;
  }

  .revolution-content {
    max-width: 56rem;
    margin: 0 auto;
    text-align: center;
  }

  .revolution-text {
    font-size: 1.1rem;
    line-height: 1.7;
    color: var(--color-text-secondary);
    margin-bottom: 2rem;
    text-align: left;
  }

  .revolution-highlight {
    background: linear-gradient(
      135deg,
      rgba(102, 126, 234, 0.1) 0%,
      rgba(118, 75, 162, 0.1) 100%
    );
    border: 1px solid rgba(102, 126, 234, 0.2);
    border-radius: 1rem;
    padding: 2rem;
    margin-top: 3rem;
  }

  .highlight-text {
    font-size: 1.25rem;
    line-height: 1.6;
    color: var(--color-text-primary);
    margin: 0;
    text-align: center;
  }

  /* Sovereignty Section */
  .sovereignty-section {
    padding: 3rem 0;
    margin-bottom: 4rem;
    text-align: center;
  }

  .sovereignty-card {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    padding: 3rem 2rem;
    border-radius: 2rem;
    box-shadow: 0 20px 40px rgba(102, 126, 234, 0.3);
  }

  .sovereignty-title {
    font-size: 2.25rem;
    font-weight: 700;
    margin: 0;
    line-height: 1.3;
  }

  /* Pillars Section */
  .pillars-section {
    padding: 4rem 0;
    margin-bottom: 4rem;
  }

  .pillars-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    max-width: 56rem;
    margin: 0 auto;
  }

  .pillar-card {
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: 1.5rem;
    padding: 3rem 2.5rem;
    transition: all 0.3s ease;
    position: relative;
    overflow: hidden;
    animation: cardSlideUp 0.8s ease-out;
    animation-fill-mode: both;
  }

  .pillar-card:nth-child(1) { animation-delay: 0.1s; }
  .pillar-card:nth-child(2) { animation-delay: 0.3s; }
  .pillar-card:nth-child(3) { animation-delay: 0.5s; }

  .pillar-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, #667eea, #764ba2);
    transform: scaleX(0);
    transition: transform 0.3s ease;
  }

  .pillar-card:hover::before {
    transform: scaleX(1);
  }

  .pillar-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 40px rgba(102, 126, 234, 0.15);
    border-color: rgba(102, 126, 234, 0.2);
  }

  .pillar-icon {
    font-size: 3rem;
    margin-bottom: 1.5rem;
    display: block;
    text-align: center;
  }

  .pillar-title {
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin-bottom: 1.5rem;
    line-height: 1.3;
    text-align: center;
  }

  .pillar-description {
    font-size: 1rem;
    line-height: 1.7;
    color: var(--color-text-secondary);
    margin-bottom: 1.5rem;
  }

  .pillar-description:last-child {
    margin-bottom: 0;
  }

  .pillar-description strong {
    color: var(--color-text-primary);
    font-weight: 600;
  }

  /* Expect Section */
  .expect-section {
    padding: 4rem 0;
    margin-bottom: 4rem;
    background: linear-gradient(
      135deg,
      rgba(102, 126, 234, 0.03) 0%,
      rgba(118, 75, 162, 0.03) 100%
    );
    border-radius: 2rem;
  }

  .expect-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
    margin-top: 2rem;
  }

  .expect-card {
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: 1rem;
    padding: 2.5rem 2rem;
    text-align: center;
    transition: all 0.3s ease;
    animation: cardSlideUp 0.8s ease-out;
    animation-fill-mode: both;
  }

  .expect-card:nth-child(1) { animation-delay: 0.2s; }
  .expect-card:nth-child(2) { animation-delay: 0.4s; }

  .expect-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 15px 35px rgba(102, 126, 234, 0.15);
    border-color: rgba(102, 126, 234, 0.2);
  }

  .expect-icon {
    font-size: 2.5rem;
    margin-bottom: 1.5rem;
    display: block;
  }

  .expect-title {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--color-text-primary);
    margin-bottom: 1rem;
    line-height: 1.3;
  }

  .expect-description {
    font-size: 1rem;
    line-height: 1.6;
    color: var(--color-text-secondary);
    margin: 0;
  }

  /* Final CTA Section */
  .final-cta {
    padding: 4rem 0;
  }

  .cta-card {
    background: linear-gradient(
      135deg,
      rgba(102, 126, 234, 0.1) 0%,
      rgba(118, 75, 162, 0.1) 100%
    );
    border: 1px solid rgba(102, 126, 234, 0.2);
    border-radius: 2rem;
    padding: 4rem 2rem;
    text-align: center;
  }

  .cta-title {
    font-size: 2.25rem;
    font-weight: 700;
    color: var(--color-text-primary);
    margin-bottom: 1rem;
    line-height: 1.2;
  }

  .cta-subtitle {
    font-size: 1.1rem;
    color: var(--color-text-secondary);
    margin-bottom: 2.5rem;
    line-height: 1.5;
  }

  /* Animations */
  @keyframes badgeSlideIn {
    from {
      opacity: 0;
      transform: translateY(-20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes titleSlideUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes subtitleSlideUp {
    from {
      opacity: 0;
      transform: translateY(25px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes ctaSlideUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes cardSlideUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .contribute-hero {
      padding: 2rem 1rem 3rem;
      margin-bottom: 3rem;
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .hero-subtitle {
      font-size: 1.25rem;
    }

    .section-title {
      font-size: 2rem;
    }

    .sovereignty-title {
      font-size: 1.875rem;
    }

    .pillars-grid {
      gap: 2rem;
    }

    .pillar-card {
      padding: 2rem 1.5rem;
    }

    .pillar-title {
      font-size: 1.25rem;
    }

    .expect-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .expect-card {
      padding: 2rem 1.5rem;
    }

    .cta-card {
      padding: 3rem 1.5rem;
    }

    .cta-title {
      font-size: 1.875rem;
    }

    .explore-btn,
    .contact-btn {
      width: 100%;
      max-width: 280px;
      justify-content: center;
    }

    .revolution-text {
      font-size: 1rem;
    }

    .highlight-text {
      font-size: 1.1rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-badge,
    .hero-title,
    .hero-subtitle,
    .hero-cta,
    .pillar-card,
    .expect-card {
      animation: none;
    }

    .explore-btn,
    .contact-btn,
    .pillar-card,
    .expect-card,
    .btn-arrow {
      transition: none;
    }

    .explore-btn::before,
    .contact-btn::before,
    .pillar-card::before {
      transition: none;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const exploreBtn = document.getElementById('explore-btn');
    const contactBtn = document.getElementById('contact-btn');

    if (exploreBtn) {
      exploreBtn.addEventListener('click', function() {
        // Scroll to the revolution section or open exploration process
        const revolutionSection = document.querySelector('.revolution-section');
        if (revolutionSection) {
          revolutionSection.scrollIntoView({ behavior: 'smooth' });
        }
      });
    }

    if (contactBtn) {
      contactBtn.addEventListener('click', function() {
        // Here you would integrate with your contact/application system
        alert('Contribution application coming soon! Please reach out via email for now.');
      });
    }
  });
</script>
